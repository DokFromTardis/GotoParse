package main

import (
	"fmt"
	"net/http"
)
type serv struct{

}
func (s*serv) ServeHTTP(rw http.ResponseWriter, r*http.Request){
	fmt.Println(r.Method, r.URL.Path)
	rw.Write(byte[]("4387bb82"))
	rw.Header().Set("Access-Control-Alow-Origin", "*")
}
func main(){
	s:= &serv
	http.ListenAndServe("0.0.0.00", s)
}
